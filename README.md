# DevOps NGINX demo

## Intro

This project helps you install nginx on linux and the show the demo page

## Steps

sudo apt-get update
sudo apt-install nginx

sudo systemctl start nginx
sudo systemctl enable nginx
